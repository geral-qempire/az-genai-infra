---
title: 'Search Latency'
description: 'An overview of metric, severity, inputs, outputs and usage.'
icon: 'bell'
---

## 1. Alert
Average search latency for the search service.

- **Metric**: `SearchLatency`
- **Namespace**: `Microsoft.Search/searchServices`
- **Aggregation**: Average
- **Operator**: GreaterThan (fires when latency exceeds threshold)
- **Evaluation Frequency**: 1 minute (PT1M)
- **Time Window**: 5 minutes (PT5M)
- **Severity**: 3

## 2. Usage
```hcl main.tf
module "search_latency_alert" {
  source = "./modules/ai-search-service/alerts/SearchLatency"

  # name defaults to alrt-lat-<search-service-name>
  resource_group_name = azurerm_resource_group.rg.name
  scopes              = [module.ai_search.search_service_id]

  # Default threshold is 5ms
  # threshold = 5

  description = "Average search latency above 5ms over 5 minutes"
  enabled     = true
  action_group_ids = []
}
```

## 3. Inputs
| Name | Type | Default | Required | Description |
|------|------|---------|:--------:|-------------|
| `name` | `string` | `null` | no | Name of the metric alert (defaults to `alrt-lat-<resource-name>`). |
| `resource_group_name` | `string` | n/a | yes | Resource group in which to create the alert. |
| `scopes` | `list(string)` | n/a | yes | List of Search Service resource IDs to monitor. |
| `threshold` | `number` | `5` | no | Latency threshold in milliseconds. Fires when above this value. |
| `description` | `string` | `null` | no | Custom description. |
| `enabled` | `bool` | `true` | no | Whether the alert is enabled. |
| `action_group_ids` | `list(string)` | `[]` | no | Action Group IDs to notify. |

## 4. Outputs
| Name | Description |
|------|-------------|
| `id` | Resource ID of the metric alert. |
| `name` | Name of the metric alert. |

## 5. Requirements
- Terraform `>= 1.12.1`
- AzureRM provider `>= 4.38.1`
- Existing Resource Group
- Existing Azure AI Search Service


